<% i18n_scope = [:projects, :questionnaire, :degree, :options] %>
<% degrees = [] %>

<% DEGREES.each do |degree| %>
  <% degrees << t(degree, :scope => i18n_scope) %>
<% end %>

<%= f.input :degree, collection: degrees, as: :radio_buttons,
  checked: @project_instance.degree %>
