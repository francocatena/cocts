<% i18n_scope = [:projects, :questionnaire, :country, :options] %>
<% options = [] %>

<% COUNTRIES.each do |country| %>
  <% options << t(country, :scope => i18n_scope) %>
<% end %>

<%= f.input :country, collection: options, selected:
  @project_instance.country %>
