<% i18n_scope = [:projects, :questionnaire, :genre, :options] %>
<% genres = [] %>

<% GENRES.each do |genre| %>
  <% genres << [t(genre, :scope => i18n_scope), genre] %>
<% end %>

<%= f.input :genre, collection: genres, as: :radio_buttons,
  checked: @project_instance.genre %>
